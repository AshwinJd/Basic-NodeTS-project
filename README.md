# Basic Node TS project boilerplate

### Install dependencies

Can use yarn or npm package manager, accordingly use specific command to build and run the project.

`yarn`

### Build

`yarn build`

### Run

`yarn start`


### Lint Check

`yarn lint`